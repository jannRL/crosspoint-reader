[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "python bot.py"
restartPolicyType = "always"

[variables]
RAILPACK_INSTALL_CMD = "pip install -r bot-requirements.txt"
